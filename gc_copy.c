#include <stdlib.h>

#include "exp.h"
#include "gc.h"
#include "alloc.h"

void gc_collect(void) {

}

struct exp *alloc_exp(enum exp_type type) {
  return NULL;
}

struct env *alloc_env(struct env *parent) {
  return NULL;
}
